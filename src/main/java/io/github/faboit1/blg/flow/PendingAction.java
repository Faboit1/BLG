package io.github.faboit1.blg.flow;

/**
 * The action the player intended to perform when the rules dialog interrupted
 * them (i.e. they clicked the Login or Register choice button but rules had
 * not yet been accepted).
 */
public enum PendingAction { LOGIN, REGISTER }
